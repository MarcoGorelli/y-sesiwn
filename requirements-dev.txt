# For the tests (tests/); the site itself needs nothing but Python.
#   pip install -r requirements-dev.txt && python -m playwright install chromium
pytest
playwright
axe-playwright-python  # the axe accessibility checker, bundled
